<%= stylesheet_link_tag 'global' %>
<%= form_for(@history) do |f| %>
  <% if @history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@history.errors.count, "error") %> prohibited this history from being saved:</h2>

      <ul>
      <% @history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :action, 'Aktion' %><br>
    <%= f.text_field :action %>
  </div>
  <div class="field">
    <%= f.label :user_id, 'Benutzer' %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :description, 'Beschreibung' %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit 'Neuen Eintrag anlegen' %>
  </div>
<% end %>
